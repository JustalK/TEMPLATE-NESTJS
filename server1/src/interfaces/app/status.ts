/**
 * The status of the app
 * */
export interface StatusApp {
  /**
   * The status of the running application
   * */
  status: string;
  /**
   * The node environment of the running application
   * */
  environment: string;
}
